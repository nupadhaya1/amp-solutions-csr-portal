import { PresentationDeck } from "@/components/presentation-deck";

export const metadata = {
  title: "AMP CSR Command Center Presentation",
  description: "A live in-app presentation for the AMP CSR portal take-home demo.",
};

export default function DemoPresentationPage() {
  return <PresentationDeck />;
}
